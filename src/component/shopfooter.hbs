<hr class="mt-5 mb-3">

<footer class="col-12">
    <nav class="row nav">

        <div class="col-2 px-0">
            {{#each footer.[0]}}
            <div class="nav-item">
                <a href={{link}} class="nav-link px-0 {{#if @last}} disabled {{/if}}">{{text}}</a>
            </div>
            {{/each}}
        </div>
        <div class="col-2 px-0">
            {{#each footer.[1]}}
            <div class="nav-item">
                <a href={{link}} class="nav-link px-0 {{#if @last}} disabled {{/if}}">{{text}}</a>
            </div>
            {{/each}}
        </div>
        <div class="col-2 px-0">
            {{#each footer.[2]}}
            <div class="nav-item">
                <a href={{link}} class="nav-link px-0 {{#if @last}} disabled {{/if}}">{{text}}</a>
            </div>
            {{/each}}
        </div>
        <div class="col-2 px-0">
            {{#each footer.[3]}}
            <div class="nav-item">
                <a href={{link}} class="nav-link px-0 {{#if @last}} disabled {{/if}}">{{text}}</a>
            </div>
            {{/each}}
        </div>
        <div class="col-2 px-0">
            {{#each footer.[4]}}
            <div class="nav-item">
                <a href={{link}} class="nav-link px-0 {{#if @last}} disabled {{/if}}">{{text}}</a>
            </div>
            {{/each}}
        </div>

    </nav>
</footer>